services:
  tellingly:
    build:
      context: .
      dockerfile: Dockerfile
    image: tellingly:latest
    container_name: tellingly
    ports:
      - "127.0.0.1:8081:80"
    restart: unless-stopped
